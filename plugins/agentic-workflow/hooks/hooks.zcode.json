{
  "description": "Agentic Workflow — state write validation, forbidden-scope guard, dangerous-bash deny, verification gate (ZCode). type:process + ${ZCODE_PLUGIN_ROOT} + timeoutMs; declared by .zcode-plugin/plugin.json \"hooks\" field. Sibling platform variants: hooks.codebuddy.json / hooks.qwencode.json / hooks.trae.json / hooks.qoder.json — same node scripts, per-platform config format. block-dangerous-bash backfills the executor bash deny-list that permissionMode/approvalMode platforms cannot express.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "process",
            "command": "node",
            "args": ["${ZCODE_PLUGIN_ROOT}/hooks/validate-state-write.js"],
            "timeoutMs": 5000,
            "statusMessage": "Agentic Workflow: validating state write…"
          },
          {
            "type": "process",
            "command": "node",
            "args": ["${ZCODE_PLUGIN_ROOT}/hooks/block-forbidden-scope.js"],
            "timeoutMs": 5000,
            "statusMessage": "Agentic Workflow: checking forbidden_scope…"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "process",
            "command": "node",
            "args": ["${ZCODE_PLUGIN_ROOT}/hooks/block-dangerous-bash.js"],
            "timeoutMs": 5000,
            "statusMessage": "Agentic Workflow: checking dangerous bash…"
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "loop_limit": 5,
        "hooks": [
          {
            "type": "process",
            "command": "node",
            "args": ["${ZCODE_PLUGIN_ROOT}/hooks/check-verification-on-stop.js"],
            "timeoutMs": 5000,
            "statusMessage": "Agentic Workflow: checking verification before stop…"
          }
        ]
      }
    ]
  }
}
