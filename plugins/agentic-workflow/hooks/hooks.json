{
  "description": "Agentic Workflow — state write validation hook",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "process",
            "command": "node",
            "args": ["${ZCODE_PLUGIN_ROOT}/hooks/validate-state-write.js"],
            "timeoutMs": 5000,
            "statusMessage": "Agentic Workflow: validating state write…"
          }
        ]
      }
    ]
  }
}
